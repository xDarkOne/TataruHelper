<UserControl x:Class="FFXIVTataruHelper.Views.Controls.SecretField"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:utils="clr-namespace:WpfXamlExtensions;assembly=WpfXamlExtensions"
             x:Name="Root">
    <UserControl.Resources>
        <Style x:Key="RevealToggleStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="{DynamicResource TextFillColorPrimaryBrush}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Padding" Value="6" />
            <Setter Property="Focusable" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background"
                                        Value="{DynamicResource SubtleFillColorSecondaryBrush}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background"
                                        Value="{DynamicResource SubtleFillColorTertiaryBrush}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <TextBox x:Name="ValueBox"
                     Text="{Binding Value, ElementName=Root, UpdateSourceTrigger=LostFocus, Mode=TwoWay}"
                     KeyDown="OnValueKeyDown"
                     Visibility="{Binding IsRevealed, ElementName=Root, Converter={utils:BooleanToVisibilityConverterCollapse}}" />

            <TextBlock Text="{Binding Masked, ElementName=Root}"
                       VerticalAlignment="Center"
                       Padding="8,6">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Visibility" Value="Visible" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsRevealed, ElementName=Root}" Value="True">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </Grid>

        <ToggleButton Grid.Column="1"
                      Margin="6,0,0,0"
                      VerticalAlignment="Center"
                      Style="{StaticResource RevealToggleStyle}"
                      IsChecked="{Binding IsRevealed, ElementName=Root, Mode=TwoWay}">
            <ToggleButton.ToolTip>
                <ToolTip>
                    <TextBlock>
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Text" Value="Show" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsRevealed, ElementName=Root}" Value="True">
                                        <Setter Property="Text" Value="Hide" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </ToolTip>
            </ToggleButton.ToolTip>
            <ui:SymbolIcon FontSize="18">
                <ui:SymbolIcon.Style>
                    <Style TargetType="ui:SymbolIcon">
                        <Setter Property="Symbol" Value="Eye24" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsRevealed, ElementName=Root}" Value="True">
                                <Setter Property="Symbol" Value="EyeOff24" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ui:SymbolIcon.Style>
            </ui:SymbolIcon>
        </ToggleButton>
    </Grid>
</UserControl>